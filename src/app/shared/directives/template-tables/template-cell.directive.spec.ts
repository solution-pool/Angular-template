/*import {CredixCellDirective} from './credix-cell.directive';

describe('CredixCellDirective', () => {
  it('should create an instance', () => {
    const directive = new CredixCellDirective();
    expect(directive).toBeTruthy();
  });
});*/
